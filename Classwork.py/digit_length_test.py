from unittest import TestCase
import digit_length

class digit_length_test(TestCase):

    def test_that_first_number_is_negative(self):
#        if(first_number < 0 and second_number <= 36):
            is_negative_number = number_is_negative (-1, 25)
            actual_number = 0
            return actual_number;

            self.assertTrue(is_negative_number, actual_number)
                
                
